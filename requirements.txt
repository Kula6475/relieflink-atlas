# One install for everything: pip install -r requirements.txt
fastapi
uvicorn[standard]
sqlmodel
requests
python-dotenv
anthropic
ortools
openpyxl
python-multipart
pytest
httpx
ruff
langgraph
langchain-anthropic

# Optional, only for the Python edge detector (the browser one at /camera needs nothing):
#   pip install ultralytics
